version: "3.9"
services:
  driller-server:
    ports:
      - "9000:9000"
    image: driller-example-server:latest
    build:
      context: ./
      dockerfile: Dockerfile-example-server
      #args:
      #  buildno: 1
    environment:
        OUTPUT: 0
        DELAY_MS: 100
